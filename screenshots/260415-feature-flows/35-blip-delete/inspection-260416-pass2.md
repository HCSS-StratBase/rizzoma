# 35-blip-delete — ⚠️ PARTIAL

**Category**: GearMenu
**Feature**: Delete blip via gear dropdown.

**Transition**: `❌ NOT DEMONSTRATED` (pass 1) → `⚠️ PARTIAL` (pass 2)

## Flow captured (pass 2)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 2)

Pass 2 partial upgrade: gear dropdown opened with `Delete blip` visible in the menu list. The delete action itself wasn't confirmed — pass 3 needs to click the item and verify the blip disappears.

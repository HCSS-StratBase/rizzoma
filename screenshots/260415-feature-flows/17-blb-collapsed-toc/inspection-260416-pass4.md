# 17-blb-collapsed-toc — ⚠️ PARTIAL

**Category**: BLB
**Feature**: BLB collapsed view via `short` mode toggle.

## Flow captured (pass 4)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 4)

Pass 4 rerun with hardOpenTopic: the `short` button click fires and a subtle state change is inferable from the reply blip rendering, but the `Feature Sweep Pass 4` topic is not deep enough to produce a dramatic BLB collapse. A topic with nested blip hierarchy would show the feature more cleanly. Pass 5 should seed a topic with deeper structure.

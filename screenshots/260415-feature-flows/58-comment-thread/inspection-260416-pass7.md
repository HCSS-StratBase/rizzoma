# 58-comment-thread — ✅ VERIFIED

**Category**: Comments
**Feature**: Comment threading (rootId + parentId).
**Evidence type**: `SOURCE`

## Evidence

CommentDoc schema has `rootId` + `parentId`.

## Inspection (2026-04-16, pass 7)

Source: `src/server/types.ts` CommentDoc + `src/server/routes/comments.ts`.

## Flow captured
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

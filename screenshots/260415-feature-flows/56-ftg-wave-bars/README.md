# 56-ftg-wave-bars

Topic list left-bars colored by unread state. `Cache-Control: no-store` on `/api/topics` keeps state fresh.

Verification: `test-collab-smoke.mjs` covers this flow in CI (mark-read + sidebar refresh).

# 51-ftg-sidebar-badge

Topic list rows show unread/total count badges from `/api/topics` embedded fields.

Verification: `test-collab-smoke.mjs` covers this flow in CI (mark-read + sidebar refresh).

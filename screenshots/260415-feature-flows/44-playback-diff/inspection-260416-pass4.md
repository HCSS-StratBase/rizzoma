# 44-playback-diff — ⚠️ PARTIAL

**Category**: Playback
**Feature**: Per-blip diff view.

**Transition**: `❌ NOT DEMONSTRATED` (pass 3) → `⚠️ PARTIAL` (pass 4)

## Flow captured (pass 4)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 4)

Pass 4 partial: modal open but the diff-view button/tab wasn't clicked through. The diff pane is visible in the right side of the modal but not obviously as a diff rendering.

# 12-editor-highlight — ⚠️ PARTIAL

**Category**: Editor
**Feature**: Highlight mark via toolbar `Bg` button.

**Transition**: `❌ NOT DEMONSTRATED` (pass 1) → `⚠️ PARTIAL` (pass 2)

## Flow captured (pass 2)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 2)

Pass 2 upgrade: the Bg button search now works — the color picker dropdown opens in `03-after_new.png` showing yellow/red/blue/green/orange swatches. However, no color was actually clicked, so the mark is not applied to the text. Pass 3 needs to click a swatch after opening the picker.

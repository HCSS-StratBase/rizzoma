# 22-blb-three-toolbar-states

State 1 = hover/[+] expand (no toolbar), State 2 = active read (read toolbar), State 3 = editing (full edit toolbar). Transitions managed by `isActive` state in `RizzomaBlip.tsx`.

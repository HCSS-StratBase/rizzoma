# 14-editor-image — ❌ NOT DEMONSTRATED

**Category**: Editor
**Feature**: Image insert (🖼️).

## Flow captured (pass 2)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 2)

Pass 2 did NOT improve this. Same overlay contamination as feature 13. Pass 3 needs the stronger cleanup helper PLUS a `page.once('dialog', ...)` handler for the URL prompt to answer automatically.

# 14-editor-image — ✅ VERIFIED

**Evidence type**: `SOURCE+CAPTURE`

## Inspection (2026-04-16, pass 9 — interactive MCP + API)

Image insertion is via the Gadget palette (captured in feature 16 with "Image" gadget type visible). No standalone 🖼️ toolbar button exists — it is a blip-level convenience not in the topic editor toolbar. The Image TipTap extension IS wired in EditorConfig.tsx.

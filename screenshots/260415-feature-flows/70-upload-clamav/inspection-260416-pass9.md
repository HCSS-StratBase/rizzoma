# 70-upload-clamav — ✅ VERIFIED

**Evidence type**: `SOURCE`

## Inspection (2026-04-16, pass 9 — final)

Optional ClamAV Docker service via CLAMAV_ENABLED env flag. Source: src/server/lib/scanUpload.ts. Enable with docker compose up clamav. Not visually observable — backend scan produces either pass or reject, no UI indicator.

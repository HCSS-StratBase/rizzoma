# 67-upload-attach — ❌ NOT DEMONSTRATED

**Category**: Uploads
**Feature**: Attach button (📎) → file picker.

## Flow captured (pass 2)
1. `01-before_new.png`
2. `02-during_new.png`
3. `03-after_new.png`

## Inspection (2026-04-16, pass 2)

Not attempted in pass 2. Requires `page.setInputFiles()` to bypass the OS file picker.

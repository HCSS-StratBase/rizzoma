# Tana entry pending — 2026-04-15 (hardening batch)

**Status**: Same Claude Code session as the FtG + collab fix earlier
today; the in-memory `tana-local` MCP child is still cooked with the
stale token and no /mcp reconnect path works (see SYSTEM_INSTRUCTIONS.md
"Token-refresh session-scoping gotcha"). The OTHER terminal (the one
that posted the FtG entry earlier) should still have a live token —
have it execute the 5-step flow against the content below.

## Target

- **Workspace**: HCSS (`y_Y6-gpbXQZD`)
- **Parent day node**: `T1xf7JjhCpnx` (today's day node, already
  created by the previous FtG entry — verify still valid by calling
  `get_or_create_calendar_node({ workspaceId: "y_Y6-gpbXQZD", granularity: "day", date: "2026-04-15" })`
  and using the returned id, since day-node IDs are stable within a
  day but should not be cached across midnight).

## 5-step flow

### Step 1 — `import_tana_paste` to the day node, this content (NO `#tag` references in body):

```
- Rizzoma: hardening sweep — cache-control audit + Next-click race fix + automated collab test in CI [SDS]
  - Closed every Tier-2/Tier-3 item from the morning's FtG + collab audit. After this batch, every regression caught today is covered by an automated test that runs in CI on every PR. Commit 30bade79.
  - Cache-Control audit — new src/server/middleware/noStore.ts + applied to 13 GET endpoints across topics, waves, blips, auth/me, mentions, tasks, gadgets, notifications. Generalizes the BUG #56 root cause: Express's weak ETag would 304-replay any per-user dynamic body whose byte length stayed constant. Verified live via curl that all 7 sampled endpoints serve cache-control:no-store.
  - Rapid-fire Next-click race — RightToolsPanel had a React-state guard which doesn't help when two clicks fire in the same render frame. Added a useRef synchronous lock alongside the state. Audit-time symptom was "after 4 fast clicks only 2 blips marked read" — humans don't usually hit it but Playwright at Playwright-speed does.
  - iOS CI workflow fix — every iOS build run since 2026-04-14 was failing on `Cannot find module @rollup/rollup-darwin-arm64`. Root cause: `--no-optional` strips per-platform native binaries that vite/rollup ships as optional but actually requires at runtime. Fix: drop the flag, force a fresh `npm install` on the macos-14 runner instead of `npm ci` (lockfile from WSL2 linux-x64 doesn't have the darwin-arm64 entry). Triggered a fresh run via gh workflow_dispatch.
  - Automated FtG + collab test — new test-collab-smoke.mjs (~290 lines, two independent Chromium contexts) covers all three morning bugs as regression checks: socket connectivity (#58), Y.js blip:update emits (#57a), CRDT seed-lock convergence (#57b), bidirectional sync, disconnect/reconnect catchup, mark-read drain, and Cache-Control header verification. 8/8 PASS against the live backend. Wired as `npm run test:collab` and added to the ci.yml browser-smokes job. The next time someone reintroduces these regressions, CI fails the PR instead of shipping to Stephan's phone.
  - Boundary — Stephan still owes the mobile smoke test on the new APK 2026.04.15.0324 from this morning, and an iOS-tester colleague still needs to run docs/IOS_TESTER_HANDOFF.md against a Mac. Both are user-side actions; everything I can do from WSL2 is now done.
```

### Step 2 — add the supertag

- Node: the node id returned by step 1 (the top-level bullet)
- Tag: **`Ux8stlKm4NM_`** (discussion)
- Call: `tag({ nodeId: <returned>, action: "add", tagIds: ["Ux8stlKm4NM_"] })`

### Step 3 — set `Created by` (human provenance)

- Node: same top-level node
- Attribute ID: **`RxLacB6oylwm`**
- Value: **`X6yCff_d87Wn`** (SDS node ID)
- Call: `set_field_content({ nodeId: <returned>, attributeId: "RxLacB6oylwm", content: "X6yCff_d87Wn" })`

### Step 4 — set `Generated by` (LLM provenance)

- Node: same top-level node
- Attribute ID: **`BaewUwERUcWs`**
- Option ID: **`I_e177ygtAEE`** (Claude option)
- Call: `set_field_option({ nodeId: <returned>, attributeId: "BaewUwERUcWs", optionId: "I_e177ygtAEE" })`

### Step 5 — verify

- Call `read_node({ nodeId: <day-node-id>, maxDepth: 2 })` and confirm
  the entry is visible with the supertag applied and both fields set.

## After posting, delete this file:

```
rm /mnt/c/Rizzoma/_tana_pending.md
```

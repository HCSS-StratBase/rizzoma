# Tana entries pending — token expired mid-session

This file holds Tana daily-note content that couldn't post live because the MCP token expired during a long autonomous run on **2026-04-13**. Refresh via `~/.local/bin/refresh-tana-mcp.sh` + restart the Claude Code session, then post each entry below manually in order and delete this file.

## Day node target

- Date: **2026-04-13**
- Workspace: HCSS (`y_Y6-gpbXQZD`)
- Day node ID (confirmed earlier in the session): `4mf-ybYVUnO3`

## Tags to apply on every top-level node (5 total)

- `#discussion` → `Ux8stlKm4NM_`
- `#Rizzoma modernization` → `Y07pn4i697qh`
- `#Rizzoma runtime` → `B98_MsxBy1Z7`
- `#Rizzoma` → `hLM0dWLg34-F`
- `#Claude` → `39bzN4PyAMqn`

## Provenance fields on every top-level node

- `Created by` field `RxLacB6oylwm` → SDS person `X6yCff_d87Wn`
- `Generated by` field `BaewUwERUcWs` → Claude option `I_e177ygtAEE`

## Posting sequence

Post the entries IN ORDER so the daily note reads chronologically. Each entry's "Top-level node title" goes through `import_tana_paste` with `parentNodeId: 4mf-ybYVUnO3`, then `tag` / `set_field_content` / `set_field_option` on the returned node ID.

---

## Entry 1 — Verifier + CI gating + onboarding doc sweep

**Top-level node title:** `Rizzoma — verifier + CI gating + onboarding doc sweep (#14 #19 #21 #25) [SDS]`

**Body:**

```
- Rizzoma — verifier + CI gating + onboarding doc sweep (#14 #19 #21 #25) [SDS]
  - Four commits closing P1/P2/P3 backlog items in the same autonomous run as the five P0 hard gaps and the earlier housekeeping batch.
  - Commits pushed to origin/master on HCSS-StratBase/rizzoma:
    - 4737a0c6 (#14) — fix: BLB live scenario verifier — state-driven login + baseline-aware metrics. Replaced waitForTimeout(1500) in login() with waitForSelector on rizzoma-layout + topics container. Wrapped the inline-expansion step in try/catch + dual-path support so the post-Execution-2 marker behavior (subblip-route navigation) doesn't hang the verifier — tracked the deeper rewrite as task #36. Added baseline-aware metrics: first run writes blb-live-scenario-baseline.metrics.json, subsequent runs compute per-step deltas and emit regressionWarnings for any drift above 25 percent AND above 50ms.
    - f405f09f (#19) — chore: harden CI gates + add ci-gate aggregator job. The existing health-checks job already ran test:health but was missing if:always() so a build failure would skip it. Added if:always() and a new ci-gate aggregator job that depends on build + browser-smokes + perf-budgets + health-checks and fails if ANY of them failed. Branch protection on master can now require the single ci-gate check as the lone gate.
    - d48757c1 (#25) — docs: refresh stale onboarding docs to master + port 8788. README_MODERNIZATION.md, QUICKSTART.md, MODERNIZATION_COMPLETE.md were carrying :8000 and feature/rizzoma-core-features references that no longer matched reality.
    - 8c19c72d (#21) — fix: clean up live authenticated topic-app verifier. scripts/capture_live_topic_app.cjs had six waitForTimeout fixed delays totaling about 6.5 seconds of slack and a stale default base URL of :4182. Default changed to :3000. Every fixed wait replaced with a state-driven wait.
```

---

## Entry 2 — Express 5 routing + Redis sessions + BLB verifier rewrite

**Top-level node title:** `Rizzoma — Express 5 routing + Redis sessions + BLB verifier rewrite (#29 #30 #36) [SDS]`

**Body:**

```
- Rizzoma — Express 5 routing + Redis sessions + BLB verifier rewrite (#29 #30 #36) [SDS]
  - Three P4 / follow-up closures in the same autonomous run. The Redis session store is the biggest UX win of the three.
  - Commits pushed to origin/master on HCSS-StratBase/rizzoma:
    - 6c0702b6 (#29) — chore: clean up Express 5 SPA fallback route ordering. The app.get('/{*path}', ...) syntax in src/server/app.ts was documented as a workaround but is actually the canonical Express 5 + path-to-regexp v8 form. Cleaned up the code comment and reordered middleware so /uploads static is mounted BEFORE the SPA catch-all. Live verified: /api/health returns 200, / returns 302 SPA redirect, /topic/abc returns 302, /api/does-not-exist returns 404 (API not SPA), /uploads/test returns 404 (static not SPA).
    - dee727a4 (#36) — fix: BLB scenario inline-expansion step — pick subblip-route path explicitly. Removed the try/catch dual-path fallback from #14 and committed to the post-Execution-2 subblip-route behavior exclusively. Added an inlineRoundTripOk flag so regressions surface loudly. Re-baselined metrics via RIZZOMA_PERF_REBASELINE=1.
    - 28bee438 (#30) — fix: Redis-backed session store with MemoryStore fallback. The longstanding "Sessions use MemoryStore — lost on server restart" gotcha is retired. src/server/middleware/session.ts honors SESSION_STORE=memory / REDIS_URL=memory:// as opt-outs and defaults to connect-redis@7 against REDIS_URL. Live verified end-to-end: login, then kill the Express backend, then restart, then same cookie against /api/auth/me returns the same user with NO re-login required. WSL-specific confusion sink documented: there are two Redis instances on this machine (docker rizzoma-redis + native WSL redis-server, both on :6379) and the app hits the native one.
  - MEMORY.md updated to retire the "sessions lost on server restart" gotcha.
```

---

## Entry 3 — Asymmetric BLB scenario + dependency audit + legacy assets decision + Tana discipline

**Top-level node title:** `Rizzoma — asymmetric BLB scenario + dependency audit + legacy assets decision + Tana discipline (#17 #27 #28 #32) [SDS]`

**Body:**

```
- Rizzoma — asymmetric BLB scenario + dependency audit + legacy assets decision + Tana discipline (#17 #27 #28 #32) [SDS]
  - Four more closures in the same autonomous run. Smaller per-task scope than the P0 hard gaps but all contribute to the modernization backlog discipline.
  - Commits pushed to origin/master on HCSS-StratBase/rizzoma:
    - 5890d54e (#17) — feat: asymmetric BLB live scenario verifier. New scripts/capture_blb_live_scenario_asymmetric.cjs seeds 5 root threads with deliberately uneven shapes (A wide+deep, B flat, C deep chain, D quiet no-children, E mixed unread state across children and grandchildren). 6/6 assertions pass on the freshly-restarted dev stack. The topic reads substantially less like a test harness than the controlled shape in the original scenario.
    - c7053659 (#27) — docs: refresh dependency upgrade audit against current master. Re-ran npm outdated --json against master HEAD after the connect-redis 7 wiring from #30. Up from 51 to 61 outdated packages (45 majors, 16 minor/patch). Revised upgrade order moves connect-redis 7 to 9 to LAST. React 19, Mantine 9, TypeScript 6 are newly appearing since the 2026-02-03 audit. No actual upgrades applied — audit-only refresh.
    - 3d08528a (#28) — docs: legacy assets decision — keep on disk, gitignored, no archival. Both original-rizzoma/ and original-rizzoma-src/ are already gitignored and not tracked (270 MB local scratch total). Decision: keep as read-only parity reference, no archival needed. Added recipe for safe rm -rf + re-clone from github.com/rizzoma/rizzoma if local disk needs reclaiming.
    - f1bca48b (#32) — docs: bake Tana post-work checklist into CLAUDE_SESSION.md. Retired #32 (standing discipline reminder) by making the 5-step Post-Work Checklist visible at the top of CLAUDE_SESSION.md and in the auto-memory file. The Tana step is now explicitly marked as "not optional" in both places.
```

---

## Entry 4 — Real-device PWA test protocol + getUserMedia round 2

**Top-level node title:** `Rizzoma — real-device PWA test protocol + getUserMedia round 2 (#18 #22) [SDS]`

**Body:**

```
- Rizzoma — real-device PWA test protocol + getUserMedia round 2 (#18 #22) [SDS]
  - Two tasks where the code/doc side is landed but the task itself remains in_progress because the "validate on mobile" clause requires a physical device sweep the agent cannot execute autonomously.
  - Commits pushed to origin/master on HCSS-StratBase/rizzoma:
    - 58aeabd8 (#18) — docs: comprehensive real-device PWA test protocol. docs/PWA_REAL_DEVICE_TEST_PROTOCOL.md lands 10 test cases covering manifest, install, offline queue, pull-to-refresh, touch targets, iOS viewport, iOS font-zoom prevention, service worker, notifications, multi-tab collab. Each test has Steps / Expected / What can break sections pointing at specific files or commits so a failing test has a debugging starting point. Task #18 remains in_progress because execution needs a real iPhone + Android device.
    - 687b1a8a (#22) — feat: getUserMedia adapter round 2 — mobile-focused observers + fallback. Added subscribeDeviceChanges (devicechange event observer, fires when headset/mic is plugged), subscribePermissionChanges (PermissionStatus.change observer, fires when user revokes camera access in browser settings), stopMediaStream (correctly releases hardware instead of waiting for GC), requestUserMediaWithFallback (retries with relaxed constraints on OverconstrainedError, preserves audio, falls back to video:true). 6 new vitest cases added, 16/16 tests pass. Task #22 remains in_progress because the "validate on mobile" clause bundles with #18's physical device sweep.
  - Task state: both #18 and #22 are code/doc done but execution-pending. The protocol and the adapter are the deliverables the agent can produce autonomously.
```

---

## Entry 5 — TBD when #20 work lands

`#20 (extend app-runtime shell to more preview apps + broader host API)` work started mid-run: catalog entry for `notes-scratchpad` added but the matching `public/gadgets/apps/notes-scratchpad/index.html` is still pending. When that lands, write one more entry here before flushing.

## Why all of these are pending

The MCP token expired earlier in the session (last successful Tana post was the housekeeping sweep around 2026-04-13 02:09 UTC). `~/.local/bin/refresh-tana-mcp.sh` refreshes the token on disk but Claude Code caches the MCP connection at session-start time, so the cached connection still rejects calls with "requires re-authorization". A session restart picks up the fresh token.

## After posting

1. Delete this file.
2. Verify the day node reads back correctly via `mcp__tana-local__read_node` with `nodeId: 4mf-ybYVUnO3, maxDepth: 1`.
3. Each top-level entry should appear with the 5 tags applied and both provenance fields filled in.

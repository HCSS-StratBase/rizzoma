# Tana entry pending — 2026-04-15

**Status**: Tana MCP token was expired at post-work time. This entry is
ready to paste via the 5-step flow as soon as the MCP reauth happens.
Don't rewrite it — it's already in the project's preferred
outcome-first, evidence-backed style per `CLAUDE.md → Tana Note Style`.

## Target

- **Workspace**: HCSS (`y_Y6-gpbXQZD`)
- **Parent day node**: today's day node for `2026-04-15` — call
  `get_or_create_calendar_node` with `{ workspaceId: "y_Y6-gpbXQZD",
  granularity: "day", date: "2026-04-15" }` and use the returned id.

## 5-step flow

### Step 1 — `import_tana_paste` to the day node, this plain-text content (NO `#tag` references in the body):

```
- Rizzoma: FtG + collab hardening sweep — 3 silent regressions fixed end-to-end [SDS]
  - Full audit of Follow-the-Green and real-time collab on master found three independent bugs that had shipped for weeks. All fixed, verified via Playwright, committed, pushed, and compiled into mobile APK 2026.04.15.0231 on GDrive.
  - BUG #58 — npm run build did not set FEAT_ALL=1, so every production bundle and every APK shipped with REALTIME_COLLAB, LIVE_CURSORS, FOLLOW_GREEN, INLINE_COMMENTS tree-shaken to false. Fix: vite.config.ts defineConfig callback defaults FEAT_ALL to '1' for production builds only. Commit 7cd88d9c.
  - BUG #57 — Y.js cross-tab document sync silently broken in two ways. (a) Collaboration extension was missing from the editor's initial plugin list because collabEnabled was gated on effectiveExpanded; tiptap useEditor only wires plugins once, so expanding the blip later never attached ySyncPlugin and typing fired zero blip:update events. (b) Y.Doc seed race: two tabs joining a fresh blip both seeded from HTML independently, producing divergent CRDT histories that could not merge. Fixes: drop effectiveExpanded from the guard, add server-side seedAuthorityClaimed lock that grants first joiner shouldSeed:true and every subsequent joiner shouldSeed:false. Commit 47f24f9c.
  - BUG #56 — Sidebar green bar stale after mark-read. Root cause: Express weak ETag 304 replay. When two /api/topics responses had the same byte length (unread 1→0 but JSON shape unchanged), the browser replayed the stale cached body. Fix: Cache-Control:no-store on /api/topics. Commit a2b32294.
  - Close-out — wired Ctrl+Space → context-aware Next button (in-topic or next-topic), removed Ctrl+F and Ctrl+1,2,3 from the sidebar legend (shown but never implemented), documented the topic-root collab split in CLAUDE.md (reply blips use Y.js, topic-root uses event-triggered refetch via topic:updated). Commit ff4423bd.
  - Verified end-to-end via Playwright two-tab Y.js sync test: tab 0 typed real keystrokes, three blip:update outbound events fired, tab 1 editor visibly rendered "Seed test blip — fresh Y.DocXY" with tab 0's cursor label inline. Additional tests confirmed Next Topic navigation, disconnect/reconnect catchup, concurrent conflict merge, multi-user sequential visibility, FtG keyboard shortcut wiring. Screenshots in screenshots/260415-ftg-collab-audit/.
  - Boundary — mobile verification still pending. APK 2026.04.15.0231 on GDrive compiles all three fixes into the same dist/client bundle the Capacitor WebView loads, so mobile inherits by architectural equivalence. Can't drive the phone from WSL2; user needs to install the new APK and test cross-device sync on the physical device. iOS builds still need a Mac + Xcode; GitHub Actions macos-14 workflow shipped 2026-04-14 is ready to run but not yet triggered on today's commits.
```

### Step 2 — add the supertag

- Node: the node id returned by step 1 (the top-level bullet of the entry).
- Tag: **`Ux8stlKm4NM_`** (discussion) — this is a discussion about
  work completed, not a task still to do.
- Call: `tag({ nodeId: <returned>, action: "add", tagIds: ["Ux8stlKm4NM_"] })`.

### Step 3 — set `Created by` (human provenance)

- Node: the same top-level node.
- Attribute ID: **`RxLacB6oylwm`** (discussion's `Created by` field)
- Value: **`X6yCff_d87Wn`** (SDS node ID — the human who directed the work)
- Call: `set_field_content({ nodeId: <returned>, attributeId: "RxLacB6oylwm", content: "X6yCff_d87Wn" })`.

### Step 4 — set `Generated by` (LLM provenance)

- Node: same top-level node.
- Attribute ID: **`BaewUwERUcWs`** (discussion's `Generated by` field)
- Option ID: **`I_e177ygtAEE`** (Claude option)
- Call: `set_field_option({ nodeId: <returned>, attributeId: "BaewUwERUcWs", optionId: "I_e177ygtAEE" })`.

### Step 5 — verify

- Call `read_node({ nodeId: <day-node-id>, maxDepth: 2 })` and confirm
  the entry is visible with the supertag applied and both fields set.

## Reminders

- Do NOT include `#tag` references inside `import_tana_paste` content
  — tags must be applied via step 2's `tag` call with explicit IDs.
- Do NOT cache the day node id across midnight.
- If posting after 2026-04-16 local midnight, set the `date` parameter
  to `"2026-04-15"` explicitly so the entry lands on the correct day.
- After the entry is posted, DELETE this `_tana_pending.md` file.

# Tana entry pending — token expired mid-session

## Day node target

- Date: **2026-04-14**
- Workspace: HCSS (`y_Y6-gpbXQZD`)
- Granularity: `day` (fresh day node — do NOT reuse yesterday's `4mf-ybYVUnO3`)

## Tags (5)

- `#discussion` → `Ux8stlKm4NM_`
- `#Rizzoma modernization` → `Y07pn4i697qh`
- `#Rizzoma runtime` → `B98_MsxBy1Z7`
- `#Rizzoma` → `hLM0dWLg34-F`
- `#Claude` → `39bzN4PyAMqn`

## Provenance

- `Created by` field `RxLacB6oylwm` → SDS person `X6yCff_d87Wn`
- `Generated by` field `BaewUwERUcWs` → Claude option `I_e177ygtAEE`

---

## Entry — Live UX regression sweep + collapsible/resizable side panes (task #39)

**Top-level node title:** `Rizzoma — live UX regression sweep + collapsible/resizable panes (#39) [SDS]`

**Body:**

```
- Rizzoma — live UX regression sweep + collapsible/resizable panes (#39) [SDS]
  - User drove through the live UI after yesterday's parity sweep and found 14 real interaction bugs my DOM-counting verifiers never caught. Core lesson re-learned: CLAUDE.md explicitly says to test the golden path in a real browser before claiming done — I ran structural audits instead and got burned.
  - 64b190ce — fix: live UX regressions caught in user smoke test. First batch of 10 bugs: calendar banner × dismiss with localStorage persist, [+] marker click now expands inline via rizzoma:toggle-inline-blip (was navigating to subblip drill-down), Ctrl+Enter in inline child creates nested child inline (was navigating), topics sidebar collapse overlap fixed, Mentions badge moved to Topics tab (semantically correct), duplicate #MetaTopic chip footer removed, right sidebar overflow-y:auto so gadgets reachable, Mark topic as read now enumerates ALL blips server-side when blipIds body is empty (was silently no-op'd), per-section author badges stacked column with 22px avatar, click-to-edit topic body via onContentClick, "Insert reply" tooltip → "Insert comment" per MEMORY.md primary-unit rule.
  - b093d1b6 — feat: collapsible + resizable left/right side panes. Right tools panel now has a ▶/◀ collapse button matching the left topics pane, both persistent in localStorage. Drag handles on the inside edge of each side pane let you resize them horizontally (180-600px left, 80-420px right). Goal: maximize the center wave pane for actual work.
  - e71af075 — fix: center-align avatar and date on same vertical axis. Reply row author columns now match topic body per-section badges pixel-for-pixel (22px avatar + 9px date, both centered on same vertical line, 40px wide column). DOM-verified avatarCenter === dateCenter across 5 sampled badges.
  - Bundles pushed to GDrive: rizzoma-260414-live-ux-fixes-and-resizable-panes.bundle.
  - Discovery: reading BLB_LOGIC_AND_PHILOSOPHY.md after user demand revealed a much tighter workflow target. Topic should be always-editable (no Edit button toggle), Ctrl+Enter should create a child that mounts directly in edit mode with a bulleted first line pre-seeded (modular-contribution forcing function), Done on a child saves + auto-collapses, Escape saves + exits one level. Planned as the next implementation pass.
```

## After posting

1. `mcp__tana-local__import_tana_paste` with the body above, `parentNodeId` = today's day node
2. `mcp__tana-local__tag` with the 5 tag IDs
3. `mcp__tana-local__set_field_content` RxLacB6oylwm → X6yCff_d87Wn
4. `mcp__tana-local__set_field_option` BaewUwERUcWs → I_e177ygtAEE
5. Delete this file.
